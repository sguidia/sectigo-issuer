apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-by-sectigoclusterissuer
spec:
  commonName: certificate-by-sectigoclusterissuer.example.com
  secretName: certificate-by-sectigoclusterissuer
  issuerRef:
    name: sectigoclusterissuer-sample
    group: sectigo.opensource.io
    kind: SectigoClusterIssuer
